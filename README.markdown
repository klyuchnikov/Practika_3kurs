Ёто мо€ практика после 3 курса бакалавриата Ё¬ћ ”л√“”

Ёто библиотека JavaScript, котора€ позвол€ет маленькую таблицу, помещаемую на экране, проецировать и перемешать на таблицы, на пор€док большие экрана.
ќсновна€ единица Ц функци€ класс TableScrollFunction.
≈го публичные элементы:
        ќбъект settings  - настройки библиотеки;
        ‘ункци€ onScroll (fx(leftX, topY))  - подписка на событие перемещени€ таблицы
        ‘ункци€ onGetNewCells(fx(array cells) Ц подписка на событие загрузки €чеек
        	cell Ц { globalX,	globalY, localX,	localY } 
        ‘ункци€ onCellMouseover(fx(event, tableX, tableY, target))  Ц подписка на событие получени€ фокуса €чейкой 
        ‘ункци€ onCellMouseout (fx(event, tableX, tableY, target))  Ц подписка на событие потери фокуса €чейкой
        ‘ункци€ getTable() Ц получение объекта DOM таблицы
        ‘ункци€ getCell(x,y) Ц получение объекта DOM €чейки	
        ‘ункци€  Load() Ц функци€ инициализаци€ работы библиотеки

ќбъект settings : 
	idDivContainer : id элемента контейнера
	tableId: префикс id таблицы
	tableClass: CSS class таблицы
	countRows: количество строк таблицы
	countColumns: количество столбцов таблицы
	idSlider_v:  id внутреннего divа который на пор€док больше внешнего вертикального diva
	idSlider_h: id внутреннего divа который на пор€док больше внешнего горизонтального diva
	idv_slider: id внешнего вертикального diva
	idh_slider: id внешнего горизонтального diva
	cellWidth: ширина €чейки
	cellHeight: высота €чейки
	GlobalRow: количество строк глобального пространства
	GlobalColumn: количество столбцов глобального пространства
	delayLoad :  задержка между скроллингом и началом подгрузки €чеек
 
